import React, { useContext } from 'react'
import { timeLeft } from '../ui/Timer'

const TeztCD = () => {
    const timeLeft = useContext(timeLeft)
    console.log(timeLeft)
  return (
    <>
    </>
  )
}

export default TeztCD